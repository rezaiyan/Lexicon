.claude/
kotlin-js-store/
.gradle/
**/build/
.idea/
.kotlin/
ios-deployment-files/
iosApp/SourcePackages/
local.properties

# Firebase config files (contain OAuth client IDs)
google-services.json
GoogleService-Info.plist

# Security - private keys and certificates
*.key
*.pem
*.p12
*.jks
*.keystore
composeApp/keystore.jks
*-service-account*.json
vokab-*.json
fastlane/*.xml
fastlane/report.xml

# Environment and secrets
.env
.env.*
*secret*
*credential*
*.bak

# TTS native binaries are tracked via Git LFS (see .gitattributes)
