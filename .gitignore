# Claude Code personal/local files (team-shared config is committed)
.claude/settings.local.json
.claude/agent-memory-local/
.claude/hooks/logs/
kotlin-js-store/
.gradle/
**/build/
.idea/
.kotlin/
ios-deployment-files/
iosApp/SourcePackages/
local.properties

# iOS private config (contains sensitive build settings)
iosApp/Configuration/Config.private.xcconfig

# Firebase config files (contain OAuth client IDs)
google-services.json
GoogleService-Info.plist

# Security - private keys and certificates
*.key
*.pem
*.p12
*.jks
*.keystore
composeApp/keystore.jks
*-service-account*.json
vokab-*.json
fastlane/*.xml
fastlane/report.xml

# Environment and secrets
.env
.env.*
*secret*
*credential*
*.bak

# TTS native binaries are tracked via Git LFS (see .gitattributes)
